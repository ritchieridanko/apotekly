import { setCookieAccessToken } from "./cookie";
import { API_BASE_URL, APP_ENV, APP_NAME } from "./env";
import { setIconSize } from "./icon";
import { getLocalRememberMe, setLocalRememberMe } from "./localStorage";

export { API_BASE_URL, APP_ENV, APP_NAME };

export {
  getLocalRememberMe,
  setCookieAccessToken,
  setIconSize,
  setLocalRememberMe,
};
